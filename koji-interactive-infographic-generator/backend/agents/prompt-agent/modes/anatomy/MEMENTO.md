# Anatomy prompt memento

Only reviewed, repeated lessons belong here.

- Never replace an explicitly requested anatomical view with a conventional default.
- Keep supported-anatomy base images label-free because the application renders overlays.
- Do not add anatomical structures the user did not request.
